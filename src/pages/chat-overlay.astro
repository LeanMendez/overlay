---
import ChatTester from '../components/overlays/ChatTester';
import '../styles/global.css';
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width" />
    <title>Chat Overlay Preview</title>
    <style>
      body {
        background: #0a0a0a;
        min-height: 100vh;
        overflow: hidden;
        /* Sample background to test glassmorphism */
        background-image:
          radial-gradient(circle at 20% 30%, rgba(255, 255, 255, 0.05) 0%, transparent 50%),
          radial-gradient(circle at 80% 70%, rgba(255, 255, 255, 0.03) 0%, transparent 50%);
      }
    </style>
  </head>
  <body>
    <ChatTester client:load />

    <!-- Info panel -->
    <div class="fixed top-8 left-8 bg-black/80 border border-white/20 p-4 max-w-sm backdrop-blur-sm pointer-events-auto">
      <h1 class="text-white text-lg font-bold mb-2">Chat Overlay Preview</h1>
      <p class="text-white/70 text-sm mb-3">
        Overlay de chat con efecto glassmorphism y animaciones suaves
      </p>
      <div class="space-y-2 text-xs text-white/60">
        <div class="flex items-start gap-2">
          <span class="text-white">✨</span>
          <span>Efecto glassmorphism (vidrio esmerilado)</span>
        </div>
        <div class="flex items-start gap-2">
          <span class="text-white">💫</span>
          <span>Animaciones de entrada suaves</span>
        </div>
        <div class="flex items-start gap-2">
          <span class="text-white">⏱️</span>
          <span>Auto-desaparición después de 15s</span>
        </div>
        <div class="flex items-start gap-2">
          <span class="text-white">🎨</span>
          <span>Detalles minimalistas integrados</span>
        </div>
      </div>
    </div>

    <!-- Usage instructions -->
    <div class="fixed top-8 right-8 bg-black/80 border border-white/20 p-4 max-w-md backdrop-blur-sm pointer-events-auto">
      <h2 class="text-white text-lg font-bold mb-2">OBS Setup</h2>
      <ol class="text-white/70 text-sm space-y-2 list-decimal list-inside">
        <li>Add Browser source in OBS</li>
        <li>Set URL to <code class="bg-white/10 px-1 text-xs">http://localhost:4321/chat/live</code></li>
        <li>Width: 1920, Height: 1080</li>
        <li>Configure position (right, left, or bottom)</li>
        <li>Integrate with your chat bot or overlay tool</li>
      </ol>
      <div class="mt-3 pt-3 border-t border-white/20">
        <p class="text-white/60 text-xs">
          💡 Use the controls below to test different configurations
        </p>
      </div>
    </div>
  </body>
</html>
